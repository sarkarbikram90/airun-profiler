apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: airun-backendconfig
  namespace: default
spec:
  healthCheck:
    type: HTTP
    requestPath: /healthz
    port: 8080
  timeoutSec: 60
