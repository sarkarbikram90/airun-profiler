apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airun-trace-pvc
  namespace: default
  labels:
    app: airun
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard-rwo
  resources:
    requests:
      storage: 20Gi
